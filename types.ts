let a: number;
let b: string;
let c: boolean;
let d: any;
let e: number[] = [1, 2, 3];
let f: any[] = [1, 'a', true];

const colorRed = 0;
const colorGreen = 1;
const colorBlue = 2;

enum Color {
  Red,
  Green,
  Blue,
}

let backgroundColor = Color.Blue;
